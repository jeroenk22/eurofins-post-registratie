[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_APP_URL = "$DEPLOY_PRIME_URL"

[dev]
  command = "npm run dev"
  targetPort = 5173
  port = 8888
  startTimeout = 60

[[edge_functions]]
  path     = "/*"
  function = "ip-guard"
